# Development server host and port
# Set DEV_HOST to your desired IP address (e.g. a LAN IP or 0.0.0.0 to bind all interfaces)
# Leave commented out to use the default (localhost)
# DEV_HOST=localhost

# Set DEV_PORT to your desired port number
# Leave commented out to use the default (5173)
# DEV_PORT=5173
